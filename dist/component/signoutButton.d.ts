import * as React from "react";
type SignOutButtonProps = {
    redirectUrl?: string;
    label?: string;
};
declare const SignOutButton: ({ redirectUrl, label, }: SignOutButtonProps) => React.JSX.Element;
export { SignOutButton };
